class DisplayMessages extends React.Component {
  // Constructor to initialize state
  constructor(props) {
    super(props); // Calling the parent class constructor with props
    this.state = {
      input: "",    // Initializing input with an empty string
      messages: []  // Initializing messages with an empty array
    };
  }

  render() {
    return <div />;  // Rendering an empty div element
  }
};
